# Progress 12 — Plan-12 Isolation Run: Shared Backbone + bs=8 + PEFT-Aware Gradient Checkpointing

**Date:** 2026-05-13
**Follows:** Plan_12_20260512.md, Progress_10_20260512.md (Option B), Progress_11_20260512.md (Option A)
**Phase:** B (V1 isolation experiment, follow-up to Plan-10 V1)

---

## Summary

The Plan-12 isolation experiment ran cleanly to 18 epochs on a separate
server (8× A6000-class, bs=8, gather=ON). The PEFT-aware gradient
checkpointing fix worked as designed: gradients flow into both adapters
without silent corruption, τ_inv tracks the same trajectory as the
separate-backbone bs=8 reference, and the model trains stably.

**Headline result — peak head R@10 = 0.6230 at ckpt_epoch12.**

This places the shared-backbone variant **above the Plan_10 §8 decision
threshold (≥ 0.62 = "shared competitive with separate")**. The 0.054
peak gap between bs=4 shared and bs=8 separate decomposes into:

- **batch-size effect: ~0.040** (closed by the Plan-12 fix going bs=4 → bs=8)
- **architecture effect: ~0.014** (residual: shared vs separate, both at bs=8)

So ~74% of the original architecture-vs-batch confound was batch-size
driven, ~26% is a real but small architectural cost. Per the §8 matrix,
this is "shared competitive — Plan 13 V2 ablations should use the
shared variant as default" because the VRAM savings and the V4
audio-swap calculus (Plan_10 Appendix A) both favor shared when the
retrieval gap is small.

---

## 1. The Plan-12 Question (restated)

Plan-10 V1 produced:

| Variant | bs | effective negatives (8 GPU gather) | peak head R@10 |
|---|---|---|---|
| Separate backbones (Option B) | 8 | 64 | **0.6370** (ckpt_epoch11) |
| Shared backbone (Option A) | 4 | 32 | 0.5870 (ckpt_epoch16-17) |

(Option A's headline R@10 oscillates noisily in 0.55-0.59 from ep 5
onwards; 0.5870 is the run-wide max, 0.5830 at ep 7.5 is an earlier
local high. See Progress_11 §"Peak vs final and the τ_inv runaway story"
for the full trajectory.)

The 0.050 peak gap was confounded: shared backbone was forced to bs=4
because the PEFT footgun (`set_adapter` flips `requires_grad`, and HF
gradient checkpointing's recompute pass reads `model.active_adapter` at
recompute time rather than at forward time) made gradient checkpointing
unsafe — and without checkpointing, bs=8 OOM'd at 47.5/47.5 GB.

Plan 12 asked: **is the 0.054 gap architecture, batch size, or both?**

The fix: a `torch.utils.checkpoint`-aware `context_fn` that snapshots
`requires_grad` on every LoRA param before each recompute, switches to
the forward-time adapter, runs the recompute, then restores the
trainability snapshot on exit. This lets gradient checkpointing run
safely, freeing the VRAM to use bs=8 (= 64 effective negatives, same as
Option B). One variable changes vs Option A: batch size. Architecture
remains shared.

---

## 2. Run Configuration

| Parameter | Value | vs Option A (bs=4 shared) | vs Option B (bs=8 separate) |
|---|---|---|---|
| Architecture | shared backbone + 2 PEFT adapters | same | different |
| Per-GPU batch | **8** | **+4** | same |
| Gradient checkpointing | **ON** (PEFT-aware `context_fn`) | **changed** | same (native) |
| Effective negatives (gather) | **64** | **+32** | same |
| Server | 8× A6000-class | same class | same class |
| Max epochs | 18 | same | same |
| LR / schedule / LoRA cfg | same as Plan-10 V1 | same | same |
| Eval cadence | every 0.5 epoch | same | same |
| W&B run | `plan10/v1_shared_bs8_grad-ckpt_8xA6000_20260512` (`dflm7lhy`) | distinct | distinct |

Run dir: `/ssd2/zhuoyuan/plan10_runs/plan12_shared_bs8_20260512_214922/`
Log: `/ssd2/zhuoyuan/plan10_logs/plan12_shared_bs8_20260512_214922.log`

Wall-clock: **~13 hr 42 min** (start 2026-05-12 21:49, end 2026-05-13 11:31).
Slower than Option B's ~7 hr because the gradient-checkpointing recompute
roughly doubles compute per step, but the bs=8 throughput (vs Option A's
bs=4) more than offsets the longer ~30-hr wall-clock Option A would
otherwise have needed at bs=4 with checkpointing on.

### Launch command (for reproducibility)

Robust-launch pattern (tmux + `PYTHONUNBUFFERED` + `tee`) per Progress_11.
Replace `<server>` with the actual host of an 8× A6000-class machine and
adjust `/ssd2/zhuoyuan/...` paths to whatever local SSD is mounted there.

```bash
ssh <server> 'TS=$(date +%Y%m%d_%H%M%S);
  RUN_DIR=/ssd2/zhuoyuan/plan10_runs/plan12_shared_bs8_${TS};
  LOG=/ssd2/zhuoyuan/plan10_logs/plan12_shared_bs8_${TS}.log;
  mkdir -p "$RUN_DIR" /ssd2/zhuoyuan/plan10_logs
  tmux kill-session -t plan12 2>/dev/null
  tmux new-session -d -s plan12 -- bash -c "
      cd ~/projects/fashion-retrieval-agent
      export PATH=~/miniconda3/envs/fashion_retrieval/bin:\$PATH
      export HF_HOME=/ssd2/zhuoyuan/hf_cache
      export WANDB_DIR=/ssd2/zhuoyuan/wandb_cache
      export FACAP_IMAGES_DIR=/ssd2/zhuoyuan/facap-images
      export PYTHONUNBUFFERED=1
      export TOKENIZERS_PARALLELISM=false
      bash scripts/run_plan10.sh --arch shared --num-gpus 8 --batch-size 8 \
          --run-dir \$RUN_DIR --max-epochs 18 --first-eval-step 5 \
          2>&1 | tee \$LOG
      echo === TMUX RUN ENDED at \$(date) === >> \$LOG
      sleep 600
  "'
```

`scripts/run_plan10.sh` forwards `--arch shared` and the rest through to
`accelerate launch --num_processes 8 src/training/train_plan10.py ...`.
The effective accelerate command this expands to:

```
accelerate launch --num_processes 8 src/training/train_plan10.py \
    --run-dir /ssd2/zhuoyuan/plan10_runs/plan12_shared_bs8_<TS> \
    --batch-size 8 --gather --arch shared --max-epochs 18 --first-eval-step 5
```

Prerequisites on the launch host:
- `conda env: fashion_retrieval` activated (Python 3.10, torch 2.4.1,
  transformers 4.56.0.dev0, peft 0.17.1).
- HF cache at `$HF_HOME` containing `models--DanJZY--Qwen2-VL-7B-Speech`
  and `models--DanJZY--Qwen2-VL-7B-Speech-LoRA`. If absent, the launcher
  will download them on first run; pre-rsync recommended for warm start.
- FACap dress slice images at `$FACAP_IMAGES_DIR`.
- W&B credentials configured (`wandb login` once per user).

### Monitoring during a run

```bash
# attach to the running tmux session
ssh <server> -t tmux attach -t plan12

# OR tail the log file from anywhere
ssh <server> 'tail -F /ssd2/zhuoyuan/plan10_logs/plan12_shared_bs8_<TS>.log'

# verify the process is alive
ssh <server> 'pgrep -af train_plan10'
```

---

## 3. Results

### 3.1 Full R@K trajectory (every 0.5-epoch eval, steps_per_epoch=838)

**Dev split (500 queries):**

| Step | Epoch | dev R@1 | dev R@5 | dev R@10 | dev R@50 | dev/loss |
|------|-------|---------|---------|----------|----------|----------|
| 0    | 0.0   | 0.0000  | 0.0000  | 0.0000   | 0.0000   | —        |
| 5    | startup | 0.0000 | 0.0000 | 0.0000  | 0.0000   | —        |
| 419  | 0.5   | 0.0000  | 0.0040  | 0.0100   | 0.0400   | —        |
| 838  | 1.0   | 0.1260  | 0.3580  | 0.4840   | 0.7520   | 1.0782   |
| 1257 | 1.5   | 0.1620  | 0.4200  | 0.5240   | 0.8100   | —        |
| 1676 | 2.0   | 0.1580  | 0.4160  | 0.5500   | 0.8120   | —        |
| 2095 | 2.5   | 0.1820  | 0.4140  | 0.5540   | 0.8300   | —        |
| 2514 | 3.0   | 0.1680  | 0.4440  | 0.5920   | 0.8360   | —        |
| 2933 | 3.5   | 0.1920  | 0.4660  | 0.6080   | 0.8540   | 0.8558   |
| 3352 | 4.0   | 0.1760  | 0.4760  | 0.5820   | 0.8440   | —        |
| 3771 | 4.5   | 0.2080  | 0.4760  | 0.6040   | 0.8500   | —        |
| 4190 | 5.0   | 0.1940  | 0.4740  | 0.5900   | 0.8540   | —        |
| 4609 | 5.5   | 0.2000  | 0.4820  | 0.6020   | 0.8600   | —        |
| 5028 | 6.0   | 0.1900  | 0.4780  | 0.6100   | 0.8620   | 0.7241   |
| 5447 | 6.5   | 0.2160  | 0.4900  | 0.6100   | **0.8700** | 0.7328 |
| 5866 | 7.0   | 0.1940  | 0.4700  | 0.5900   | 0.8560   | 0.7405   |
| 6285 | 7.5   | 0.1880  | 0.4840  | 0.6220   | 0.8480   | 0.7385   |
| 6704 | 8.0   | 0.2060  | 0.4940  | 0.6120   | 0.8640   | 0.6976   |
| 7123 | 8.5   | 0.2100  | 0.5160  | 0.6340   | 0.8500   | 0.6778   |
| 7542 | 9.0   | 0.1820  | 0.4920  | 0.6320   | 0.8600   | 0.6907   |
| 7961 | 9.5   | 0.2000  | 0.4920  | **0.6380** | 0.8620 | 0.6998   |
| 8380 | 10.0  | 0.2060  | 0.4880  | 0.6300   | 0.8580   | 0.6759   |
| 8799 | 10.5  | **0.2320** | 0.4800 | 0.6340 | 0.8640   | 0.6714   |
| 9218 | 11.0  | 0.1960  | 0.4620  | 0.6000   | 0.8400   | 0.7097   |
| 9637 | 11.5  | 0.2120  | 0.4840  | 0.6220   | 0.8320   | 0.6722   |
| 10056 | 12.0 | 0.2140  | 0.4700  | 0.6020   | 0.8480   | 0.6745   |
| 10475 | 12.5 | 0.2160  | 0.4700  | 0.6060   | 0.8600   | 0.6735   |
| 10894 | 13.0 | 0.1940  | 0.4600  | 0.5860   | 0.8360   | 0.6573   |
| 11313 | 13.5 | 0.2220  | 0.4820  | 0.6000   | 0.8540   | 0.6557   |
| 11732 | 14.0 | 0.1800  | 0.4340  | 0.5980   | 0.8340   | 0.6937   |
| 12151 | 14.5 | 0.1900  | 0.4780  | 0.6000   | 0.8540   | 0.6648   |
| 12570 | 15.0 | 0.2060  | 0.4480  | 0.5940   | 0.8620   | 0.6597   |
| 12989 | 15.5 | 0.2240  | 0.4800  | 0.6040   | 0.8560   | 0.6695   |
| 13408 | 16.0 | 0.1920  | 0.4560  | 0.5740   | 0.8320   | 0.6835   |
| 13827 | 16.5 | 0.2000  | 0.4880  | 0.5980   | 0.8540   | **0.6329** ← min |
| 14246 | 17.0 | 0.1780  | 0.4760  | 0.6080   | 0.8380   | 0.6562   |
| 14665 | 17.5 | 0.1960  | 0.4680  | 0.6100   | 0.8520   | 0.6515   |
| 15084 | 18.0 final | 0.1820 | 0.4600 | 0.5880 | 0.8460 | 0.6787 |

**Headline split (1000 queries — the primary comparison metric):**

| Step | Epoch | head R@1 | head R@5 | head R@10 | head R@50 |
|------|-------|----------|----------|-----------|-----------|
| 0    | 0.0   | 0.0000   | 0.0000   | 0.0000    | 0.0040    |
| 5    | startup | 0.0000 | 0.0000   | 0.0000    | 0.0010    |
| 419  | 0.5   | 0.0000   | 0.0010   | 0.0050    | 0.0190    |
| 838  | 1.0   | 0.1420   | 0.3270   | 0.4350    | 0.7400    |
| 1257 | 1.5   | 0.1640   | 0.3840   | 0.5010    | 0.7910    |
| 1676 | 2.0   | 0.1650   | 0.3850   | 0.5000    | 0.7850    |
| 2095 | 2.5   | 0.1880   | 0.4270   | 0.5380    | 0.8080    |
| 2514 | 3.0   | 0.1850   | 0.4300   | 0.5490    | 0.8220    |
| 2933 | 3.5   | 0.1870   | 0.4470   | 0.5690    | 0.8490    |
| 3352 | 4.0   | 0.1990   | 0.4400   | 0.5650    | 0.8300    |
| 3771 | 4.5   | 0.2130   | 0.4730   | 0.5910    | 0.8440    |
| 4190 | 5.0   | 0.2000   | 0.4680   | 0.5940    | 0.8360    |
| 4609 | 5.5   | 0.1950   | 0.4550   | 0.5720    | 0.8330    |
| 5028 | 6.0   | 0.1940   | 0.4540   | 0.5840    | 0.8350    |
| 5447 | 6.5   | 0.2080   | 0.4700   | 0.6080    | **0.8540** |
| 5866 | 7.0   | 0.2130   | 0.4980   | 0.6060    | 0.8430    |
| 6285 | 7.5   | 0.2060   | 0.4810   | 0.6100    | 0.8470    |
| 6704 | 8.0   | 0.2120   | 0.4500   | 0.5770    | 0.8330    |
| 7123 | 8.5   | 0.2140   | 0.4920   | 0.6040    | 0.8500    |
| 7542 | 9.0   | 0.2050   | 0.4770   | 0.6010    | 0.8300    |
| 7961 | 9.5   | 0.2100   | 0.4870   | 0.6160    | 0.8540    |
| 8380 | 10.0  | 0.2050   | 0.4750   | 0.6070    | 0.8370    |
| 8799 | 10.5  | 0.2010   | 0.5010   | 0.6140    | 0.8570    |
| 9218 | 11.0  | 0.1980   | 0.4550   | 0.5830    | 0.8370    |
| 9637 | 11.5  | 0.2140   | 0.4780   | 0.5930    | 0.8430    |
| **10056** | **12.0** | 0.2100 | 0.4940 | **0.6230 ← peak** | 0.8470 |
| 10475 | 12.5 | **0.2240** | 0.4900 | 0.6110 | 0.8490    |
| 10894 | 13.0 | 0.2090   | 0.4810   | 0.6060    | 0.8350    |
| 11313 | 13.5 | 0.2130   | 0.4940   | 0.6180    | 0.8420    |
| 11732 | 14.0 | 0.2050   | 0.4820   | 0.5940    | 0.8340    |
| 12151 | 14.5 | 0.2090   | 0.4780   | 0.5970    | 0.8400    |
| 12570 | 15.0 | 0.2020   | 0.4580   | 0.5880    | 0.8300    |
| 12989 | 15.5 | 0.2140   | 0.4740   | 0.5970    | 0.8390    |
| 13408 | 16.0 | 0.1870   | 0.4750   | 0.5970    | 0.8280    |
| 13827 | 16.5 | 0.2060   | 0.4790   | 0.6060    | 0.8300    |
| 14246 | 17.0 | 0.1760   | 0.4610   | 0.5760    | 0.8280    |
| 14665 | 17.5 | 0.2100   | 0.4790   | 0.6080    | 0.8360    |
| 15084 | 18.0 final | 0.1930 | 0.4660 | 0.5870 | 0.8290  |

Bolded entries are within-column peaks. The headline R@10 peak (0.6230)
is at ckpt_epoch12; the headline R@5 / R@1 peaks come slightly later
(R@1 = 0.2240 at ep 12.5, R@5 = 0.5010 at ep 10.5), and the headline R@50
peak (0.8570) is at ep 10.5 — so the model peaks on R@10 by ep 12 but the
recall-50 of the ranking saturates earlier. Same general pattern as
Option B (peak R@10 at ep 11, peak R@50 earlier).

### 3.2 Peak vs final

| Checkpoint | head R@1 | head R@5 | head R@10 | head R@50 |
|---|---|---|---|---|
| **ckpt_epoch12 (peak on R@10)** | 0.2100 | 0.4940 | **0.6230** | 0.8470 |
| ckpt_epoch18 (final) | 0.1930 | 0.4660 | 0.5870 | 0.8290 |
| Δ (peak → final) | -0.017 | -0.028 | **-0.036** | -0.018 |

Drift on every metric, with R@10 dropping the most in absolute terms. Same dev-loss /
R@K decoupling pattern as Option B (peak ep 11, final ep 18, drift -0.064)
and Option A bs=4 (peak ep 7.5, final ep 18, drift -0.019). All three
V1 configurations overfit on the retrieval metric past their respective
peaks while dev/loss kept slowly improving.

---

## 4. Decomposition: batch size vs architecture

Working backwards from the three V1 data points:

| Run | Arch | bs | effective negs | peak head R@10 |
|---|---|---|---|---|
| Option A | shared | 4 | 32 | 0.5870 |
| **Plan 12** | **shared** | **8** | **64** | **0.6230** |
| Option B | separate | 8 | 64 | 0.6370 |

**Holding architecture constant (shared), bs=4 → bs=8 lift:**
0.6230 − 0.5870 = **+0.036 head R@10** = batch-size effect

**Holding batch size constant (bs=8), shared → separate lift:**
0.6370 − 0.6230 = **+0.014 head R@10** = architecture effect

**Total gap (the original Option A vs Option B confound):**
0.6370 − 0.5870 = 0.050 = 0.036 + 0.014 ✓

So:
- **~72%** of the original gap was batch-size.
- **~28%** of the gap is the architectural cost of two adapters sharing
  one frozen substrate vs each tower owning its own backbone.

The architectural cost is real but small. Plan_10 Appendix A's audio-swap
argument for the shared variant (when V4 swaps the query side to audio,
the shared variant only retrains one adapter and keeps the target
adapter; the separate variant retrains the full query backbone) is now
~0.014 head R@10 worth of trade-off — well within the noise of any V2
ablation lift.

---

## 5. τ_inv: the fix worked exactly as designed

The diagnostic claim from Progress_10 §10 (dev-loss / R@K decoupling driven
by τ_inv runaway with too-few negatives) predicted that bs=8 should
stabilize τ_inv. Plan 12 confirms this empirically:

| Run | bs | effective negs | init τ_inv | final τ_inv | Δ over 18 ep |
|---|---|---|---|---|---|
| Option A (shared) | 4 | 32 | 14.5 | 23.50 | +9.00 |
| **Plan 12 (shared)** | **8** | **64** | **14.3** | **18.40** | **+4.10** |
| Option B (separate) | 8 | 64 | 14.5 | 18.34 | +3.84 |

Plan 12's τ_inv tracks Option B's almost identically. This is the
load-bearing safety check on the PEFT-aware context_fn: if the recompute
had been routing gradients into the wrong adapter, the trainable τ_inv
parameter would not have evolved this cleanly — we'd see anomalous
trajectory, NaN spikes, or a degenerate stall.

The fix is correct.

---

## 6. Headline comparison vs all anchors

| Method | head R@1 | head R@5 | head R@10 | head R@50 |
|---|---|---|---|---|
| Caption + MiniLM (Plan-3 anchor) | 0.084 | — | 0.240 | — |
| Caption + Marqo FashionCLIP (Phase-A baseline) | 0.258 | 0.456 | 0.533 | 0.685 |
| Qwen2VL → Marqo FashionCLIP (Plan-6 best) | — | — | 0.402 | 0.646 |
| Plan-10 V1 Option A shared bs=4 (R@10 max, ckpt_ep16-17) | 0.1970 | 0.4480 | 0.5870 | 0.8140 |
| **Plan-12 shared bs=8 + PEFT ckpt fix (peak ckpt_ep12)** | **0.2100** | **0.4940** | **0.6230** | **0.8470** |
| Plan-10 V1 Option B separate bs=8 (peak ckpt_ep11) | 0.222 | 0.522 | **0.6370** | 0.842 |

Plan-12 peak vs anchors:
- **vs Phase-A baseline (0.533)**: +0.090 absolute / +16.9% relative on head R@10
- **vs Plan-6 contrastive (0.402)**: +0.221 absolute / +55.0% relative
- vs Option B peak (0.6370): -0.014 absolute / -2.2% relative

The headline takeaway for the resume / writeup remains tied to **Option B
(separate backbones, head R@10 = 0.6370)** because that is the
project's strongest single number. Plan-12 is supporting evidence that
the shared variant is competitive and is the right baseline architecture
for V2 ablations going forward.

---

## 7. §8 Decision Matrix Outcome (Plan_12)

Plan_12 §8 thresholds (head R@10 at peak):

| Threshold | Outcome | Interpretation |
|---|---|---|
| ≥ 0.62 | **Plan 12 peak = 0.6230 → here** | Batch size confound confirmed. Shared backbone is competitive when matched on effective negatives. Plan 13 default = shared. |
| 0.58 - 0.62 | — | Partial — would have required a bs=12 / bs=16 datapoint. |
| < 0.58 | — | Architecture dominates. |

**Decision:** Plan 13 V2 ablations will use the **shared backbone** as
the default architecture, with the following levers (V2 / V3 from
Plan_10 §3 + new levers from Progress_10 §10):

1. **τ_inv clip** at ~15-16 (where peaks landed). Prevents the late-stage
   loss-metric decoupling that costs ~0.06 head R@10.
2. **Cosine LR decay** or **early stop on head R@10** (not dev/loss —
   they diverge past the peak).
3. **Trainable-surface ablation** (V2): heads-only, query-LoRA-only,
   larger rank, unfrozen MLP — all on the shared variant.
4. **Target-prompt sweep** (V3): plain / fashion-attribute list / empty,
   replacing the locked "Describe this image in detail." from Plan_10 §4.2.
5. **Hard-negative mining** (Plan-7 unblocked next lever): in-batch
   negatives are currently random; hard negatives from the gallery would
   push past the current ceiling.

V4 audio extension (swap query side to speechQwen2VL) is now justified
on architecture grounds — the shared variant's swap is a single-adapter
retrain.

---

## 8. Artifacts

### Run directory contents (server 11, /ssd2)

```
/ssd2/zhuoyuan/plan10_runs/plan12_shared_bs8_20260512_214922/
  ckpt_epoch{1..18}/
    shared_backbone/      # one PEFT model with two named adapters
                          # ("query", "target") — both saved here
    head_query.pt
    head_target.pt
    logit_scale.pt
  gallery_emb_epoch{0..18}.npy        # 19 × 116 MB = 2.2 GB
  gallery_emb_epoch{0..18}.meta.json
  dev_slice.json
  metrics.json                        # final eval JSON
```

Per-checkpoint: ~120-140 MB (one PEFT dir holds both adapters as keyed
safetensors; smaller than Option B's per-ckpt since one shared backbone).

W&B: `plan10/v1_shared_bs8_grad-ckpt_8xA6000_20260512` (run ID `dflm7lhy`).

### Best checkpoint

`ckpt_epoch12` — head R@10 = 0.6230. Recommended retention. Apply the
same peak ± 2 cleanup pattern (keep ep 10-14, delete 1-9 and 15-18) once
Plan 13 lever choices are committed.

---

## 9. Pending

- **Cleanup** (defer): apply peak ± 2 cleanup to Plan-12 run dir once
  Plan 13 levers are nailed down. Current footprint: ~3 GB
  (~120 MB × 18 ckpts + 2.2 GB galleries). Saves ~2 GB.
- **Qualitative dump** from `ckpt_epoch12` per Plan_10 §7.3 — not yet
  generated. Useful when Plan 13 demos compare shared-bs8 against
  separate-bs8 on the same query slice.
- **Plan 13** writeup — V2 trainable-surface + V3 target-prompt + τ_inv
  clip + cosine LR + hard-negative mining levers on the shared variant.
- **Cross-server checkpoint rsync** of `ckpt_epoch12` to the home server
  (long-term archive). Defer until Plan 13 lever-choice decisions are
  committed so we don't rsync a soon-to-be-superseded model.

---

## 10. Files touched in this work

| File | Status | Note |
|---|---|---|
| `src/training/two_tower_model.py` | Modified (not yet committed) | Added `make_adapter_restoring_context_fn` + wired into `TwoTowerSharedBackbone.__init__` |
| `src/training/train_plan10.py` | Modified (not yet committed) | Added `args.arch == "shared"` branch to `--resume-from` loader (lines 315-336) |
| `Documentation/Plan_12_20260512.md` | Modified (not yet committed) | 3 rounds of cross-review revisions |
| `Documentation/Progress_12_20260513.md` | Created | This file |

---

## 11. Follow-up: Batch-size scan + Plan-13 bs=24 run (2026-05-13/14)

### 11.1 The scaling experiment

With the PEFT-aware gradient checkpointing fix landing the shared variant at peak head R@10 = 0.6230 at bs=8 (this doc's main result), the natural follow-up was: *how much further can we push?* The fix freed substantial VRAM that Plan-12 didn't use. Plan-12 at bs=8 sat at ~33 GB / 48 GB per GPU — 15 GB of headroom.

We scanned **bs ∈ {16, 24}** with a 1-epoch stability gate at each step (validate model load + step-0 backward + end-of-epoch refresh + post-refresh eval all complete without OOM). bs=32 was not attempted because the bs=24 scan revealed memory was already creeping toward the safety threshold.

### 11.2 Memory at each batch size

`nvidia-smi memory.used` — peak across all 8 ranks during steady-state training. (See `Documentation/notes_progress_10_two_tower_training.md` Q6/Q7 for why this number is a coarse step function of bs and why ranks differ.)

| bs | effective negatives (×8 GPUs) | peak per-rank memory | headroom to 48 GB | verdict |
|---|---|---|---|---|
| 8 (Plan-12) | 64 | ~33 GB | ~15 GB | comfortable |
| 16 | 128 | ~33 GB | ~15 GB | comfortable (allocator didn't expand) |
| 24 | 192 | **44.8 GB** (rank 6) | ~3.2 GB | tight but stable through 18 epochs |
| 32 (not attempted) | 256 | (projected ~48+) | ≤0 | likely OOM, skipped |

bs=24 was selected for the full run despite the thin margin because the memory creep proved to be one-shot (allocator grabbed an extra chunk in epoch 4 and stabilized).

### 11.3 Run config (Plan-13 bs=24)

| Parameter | Value | vs Plan-12 bs=8 |
|---|---|---|
| Architecture | shared backbone + 2 PEFT adapters | same |
| Per-GPU batch | **24** | **+16** |
| Gradient checkpointing | ON (PEFT-aware `context_fn`) | same |
| Effective negatives | **192** | **3× more** |
| Max epochs | 18 | same |
| All other hyperparams (LR, schedule, LoRA cfg, eval cadence) | same | same |
| W&B run | `plan10/v1_shared_bs24_8xA6000_20260513` | distinct |

Run dir: `/ssd2/zhuoyuan/plan10_runs/plan13_shared_bs24_20260513_202739/`
Log: `/ssd2/zhuoyuan/plan10_logs/plan13_shared_bs24_20260513_202739.log`

Wall-clock: ~14 hr 15 min (start 2026-05-13 20:27, end 2026-05-14 ~10:42).

### 11.4 Results — new project-wide best

**Peak head R@10 = 0.6540 at ckpt_epoch14 (step 3906).**

Full eval at the peak checkpoint:

| Split | R@1 | R@5 | R@10 | R@50 |
|---|---|---|---|---|
| **dev (500q)** | 0.2500 | 0.5180 | 0.6620 | 0.8900 |
| **headline (1000q)** | 0.2310 | 0.5280 | **0.6540** | 0.8660 |

Full eval at ckpt_epoch18 (final):

| Split | R@1 | R@5 | R@10 | R@50 |
|---|---|---|---|---|
| dev | 0.2360 | 0.5240 | 0.6360 | 0.8740 |
| headline | 0.2170 | 0.5160 | 0.6230 | 0.8530 |

Drift peak (ep 14) → final (ep 18) on head R@10: **-0.031** (cleaner than Plan-12's -0.036 and much cleaner than Option B's -0.064).

### 11.5 Full headline R@10 trajectory at end-of-epoch boundaries

| Epoch | Plan-12 (bs=8 shared) | Option B (bs=8 separate) | **Plan-13 (bs=24 shared)** | Δ Plan-13 vs Option B |
|---|---|---|---|---|
| 1 | 0.4350 | 0.4680 | 0.4600 | -0.008 |
| 2 | 0.5000 | 0.5410 | 0.5260 | -0.015 |
| 3 | 0.5890 | 0.5780 | 0.5720 | -0.006 |
| 4 | 0.5780 | 0.5780 | 0.5960 | +0.018 |
| 5 | 0.6170 | 0.6170 | 0.6270 | +0.010 |
| 6 | 0.6130 | 0.6130 | 0.6290 | +0.016 |
| 7 | 0.6160 | 0.6160 | 0.6420 | +0.026 |
| 8 | 0.6080 | 0.6080 | 0.6390 | +0.031 |
| 9 | 0.6190 | 0.6190 | 0.6440 | +0.025 |
| 10 | 0.6260 | 0.6260 | 0.6450 | +0.019 |
| 11 | 0.5830 | **0.6370 ← B peak** | 0.6300 | -0.007 |
| 12 | **0.6230 ← P12 peak** | 0.6160 | 0.6450 | +0.029 |
| 13 | 0.6060 | 0.6190 | 0.6350 | +0.016 |
| **14** | 0.5940 | 0.6110 | **🏆 0.6540 ← P13 peak** | **+0.043** |
| 15 | 0.5880 | 0.5970 | 0.6420 | +0.045 |
| 16 | 0.5880 | 0.5880 | 0.6360 | +0.048 |
| 17 | 0.5760 | 0.6120 | 0.6380 | +0.026 |
| 18 final | 0.5870 | 0.5730 | 0.6230 | +0.050 |

Plan-13 (bs=24) is **at or above Option B from ep 4 onward**, and the late-training divergence is striking — by ep 18 the gap is +0.050 R@10, even though all three runs share the same architecture-equivalent peak design. The shared-backbone variant with 192 effective negatives **overfits less aggressively** than either bs=8 variant.

### 11.6 τ_inv — the mechanism explained

| Run | bs | effective negs | init τ_inv | final τ_inv | Δ over 18 ep |
|---|---|---|---|---|---|
| Option A (shared) | 4 | 32 | 14.5 | 23.50 | **+9.00** |
| Option B (separate) | 8 | 64 | 14.5 | 18.34 | +3.84 |
| Plan-12 (shared) | 8 | 64 | 14.3 | 18.40 | +4.10 |
| **Plan-13 (shared)** | **24** | **192** | **14.3** | **15.53** | **+1.23** |

Plan-13's τ_inv climbed by only +1.23 over 18 epochs vs Plan-12's +4.10 and Option A's +9.00. **More negatives = much less τ runaway** — this is the dev-loss / R@K decoupling mechanism documented in Progress_10 §10 working as predicted. With less runaway, the model doesn't trade R@K for sharper-softmax loss reduction, so the late-training R@K stays high.

### 11.7 Updated headline comparison vs all anchors

| Method | head R@1 | head R@5 | head R@10 | head R@50 |
|---|---|---|---|---|
| Caption + MiniLM (Plan-3 anchor) | 0.084 | — | 0.240 | — |
| Caption + Marqo FashionCLIP (Phase-A baseline) | 0.258 | 0.456 | 0.533 | 0.685 |
| Qwen2VL → Marqo FashionCLIP (Plan-6 best) | — | — | 0.402 | 0.646 |
| Plan-10 V1 Option A (shared bs=4 peak) | 0.1970 | 0.4480 | 0.5870 | 0.8140 |
| Plan-12 (shared bs=8 peak) | 0.2100 | 0.4940 | 0.6230 | 0.8470 |
| Plan-10 V1 Option B (separate bs=8 peak) | 0.2220 | 0.5220 | 0.6370 | 0.8420 |
| **Plan-13 (shared bs=24) peak ckpt_ep14** | **0.2310** | **0.5280** | **🏆 0.6540** | **0.8660** |
| Plan-13 (shared bs=24) final ckpt_ep18 | 0.2170 | 0.5160 | 0.6230 | 0.8530 |

Plan-13 peak vs all anchors:
- **vs Marqo FashionCLIP (0.533)**: +0.121 absolute / **+22.7% relative** on head R@10
- vs prior project best Option B (0.6370): +0.017 / +2.7% on head R@10
- vs Plan-6 contrastive (0.402): +0.252 / +62.7%

Best head R@K across all metrics achieved by Plan-13 (peak ckpt). The result is dominated on every column — R@1, R@5, R@10, R@50 — vs every prior anchor.

### 11.8 Launch command (Plan-13 bs=24 reproducibility)

Same robust-launch pattern as §2:

```bash
ssh <server> 'TS=$(date +%Y%m%d_%H%M%S);
  RUN_DIR=/ssd2/zhuoyuan/plan10_runs/plan13_shared_bs24_${TS};
  LOG=/ssd2/zhuoyuan/plan10_logs/plan13_shared_bs24_${TS}.log;
  mkdir -p "$RUN_DIR" /ssd2/zhuoyuan/plan10_logs
  tmux new-session -d -s plan13 -- bash -c "
      cd ~/projects/fashion-retrieval-agent
      export PATH=~/miniconda3/envs/fashion_retrieval/bin:\$PATH
      export HF_HOME=/ssd2/zhuoyuan/hf_cache
      export WANDB_DIR=/ssd2/zhuoyuan/wandb_cache
      export FACAP_IMAGES_DIR=/ssd2/zhuoyuan/facap-images
      export PYTHONUNBUFFERED=1
      export TOKENIZERS_PARALLELISM=false
      bash scripts/run_plan10.sh --arch shared --num-gpus 8 --batch-size 24 \
          --run-dir \$RUN_DIR --max-epochs 18 --first-eval-step 5 \
          2>&1 | tee \$LOG
      echo === TMUX RUN ENDED at \$(date) === >> \$LOG
      sleep 600
  "'
```

The only difference vs Plan-12: `--batch-size 24` instead of 8. No code changes, no plan revision — pure hyperparameter scaling on the bs lever made possible by the gradient checkpointing fix.

### 11.9 Implications for Plan 13 V2 ablations

The original Plan-12 §7 §8 decision matrix recommended "Plan 13 V2 ablations using the shared variant as default with τ-clipping, cosine LR, early stop, trainable-surface ablation, target-prompt sweep, hard-negative mining." Plan-13 bs=24's result modifies the priority:

1. **bs=24 already delivers the τ-stability benefit** that the τ-clip lever was meant to provide. τ_inv only climbed +1.23 over 18 epochs. **τ-clip may not be needed** at bs=24.
2. **Early stop on head R@10** still matters — peak at ep 14, final at ep 18, drift -0.031. Could save ~4 epochs of training while keeping the best ckpt.
3. **Cosine LR decay** is still untested at bs=24; might help further by slowing late-stage updates.
4. **Trainable-surface ablation, target-prompt sweep, hard-negative mining** are independent of bs and remain top V2 priorities.

The bs=24 result is the new starting point for any V2 ablation — they should be measured as deltas off head R@10 = 0.6540, not off Plan-12's 0.6230.
