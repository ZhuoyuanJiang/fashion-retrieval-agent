# Progress 2 — Plan 2 Local Baseline Scaffolding

Execution log for `Documentation/Plan_2_20260427.md`. Covers Milestones
1–3 (M4 deferred to a separate round). All work done locally on an
8 GB-VRAM laptop with the real VLM backends stubbed for the server.

Run date: 2026-04-27.
Final smoke results: oracle R@1 = 1.0000, mock exits clean.
Real backends raise `RuntimeError("server-only: ...")` on this machine.

---

## System design

The text-modification retrieval baseline (**Method A**): turn
`(reference image + modification text)` into an "imagined target
caption" via a VLM, then retrieve via text-to-text similarity against
pre-encoded target captions. Each file under `src/` does **one** step
of that pipeline.

### Per-query flow (one FACap triplet)

```
                ┌──────────────────────────────────────────────┐
                │ FacapDataset[i] -> 6-key dict:               │
                │   candidate_image_path                       │
                │   modification_text                          │   step 0
  src/data/     │   target_image_path                          │  "iterate
  facap_dataset │   target_caption                             │   triplets"
                │   target_id                                  │
                │   candidate_id                               │
                └──────────────────┬───────────────────────────┘
                                   │ item dict
                                   v
                ┌──────────────────────────────────────────────┐
                │ VLMCaptioner.caption(item) -> str            │   step 1
  src/baseline/ │                                              │  "imagine
  vlm_caption   │   backends: Mock | Oracle | Qwen2VL |        │   the
                │             SpeechQwen2VL                    │   target"
                └──────────────────┬───────────────────────────┘
                                   │ generated_caption (str)
                                   v
                ┌──────────────────────────────────────────────┐
                │ TextEncoder.encode([caption])                │   step 2
  src/baseline/ │   -> np.ndarray (1, 384) float32             │  "vectorize
  text_encoder  │      L2-normalized                           │   the imagined
                │                                              │   caption"
                └──────────────────┬───────────────────────────┘
                                   │ query embedding
                                   v
                ┌──────────────────────────────────────────────┐
                │ retrieve.top_k(q_emb, db, k=10)              │
  src/baseline/ │   -> [(target_id, score), ...]               │   step 3
  retrieve      │ retrieve.rank_of(true_id, q_emb, db)         │  "find
                │   -> int (1-based rank, or None)             │   nearest
                │                                              │   captions"
                └──────────────────┬───────────────────────────┘
                                   │ top-K + true rank
                                   v
                ┌──────────────────────────────────────────────┐
                │ eval.compute_metrics(ranks)                  │   step 4
  src/baseline/ │   -> Recall@1/5/10/50 + median + mean rank   │  "score
  eval          │ eval.write_qualitative(rows) -> JSONL        │   it"
                │ eval.write_metrics(result) -> metrics.json   │
                └──────────────────────────────────────────────┘
```

### Pre-step (run once per `--run-name`)

```
                ┌──────────────────────────────────────────────┐
                │ build_db():                                  │
                │   pick captions (eval targets + distractors) │  pre-step
  src/baseline/ │   encode all of them via TextEncoder         │  "build the
  build_caption_│   write embeddings.npy + metadata.jsonl +    │   haystack
  db            │         config.json (with build_args + SHA)  │   once"
                └──────────────────────────────────────────────┘
```

### One-time helper (only for real-VLM runs on the server)

```
                ┌──────────────────────────────────────────────┐
                │ prepare_images():                            │
  src/baseline/ │   stream Marqo/fashion200k, save eval-slice  │  "no surprise
  prepare_images│   candidate images to local cache before     │   network
                │   inference starts                           │   mid-run"
                └──────────────────────────────────────────────┘
```

### The orchestrator that wires it all

```
                ┌──────────────────────────────────────────────┐
                │ run_baseline.py main():                      │
                │   ensure_caption_db (auto-build + 2 stale    │
                │     gates: build_args & FACap-SHA)           │  "one
  src/baseline/ │   for each eval triplet:                     │   command
  run_baseline  │     captioner.caption -> encoder.encode ->   │   runs
                │       retrieve.top_k + retrieve.rank_of      │   the
                │   eval.compute_metrics + write_metrics +     │   whole
                │     write_qualitative                        │   thing"
                │   print metrics table                        │
                └──────────────────────────────────────────────┘
```

### "We wanted X, so we built Y"

| What we wanted | What we built |
|---|---|
| Iterate FACap triplets without loading images we won't use | `facap_dataset.py` returns image **paths** in the dict; `load_image()` opens lazily |
| Same text encoder on both sides of cosine similarity (DB and query), CPU-only so VRAM stays free | `text_encoder.py` — thin SBERT wrapper, `device="cpu"`, L2-normalized output |
| A reusable retrieval index of target captions | `build_caption_db.py` writes `embeddings.npy + metadata.jsonl + config.json` |
| Smoke DB whose results are interpretable (no "did Recall@K fail because the target wasn't in the DB?") | DB construction = (eval targets) ∪ (random distractors), so eval targets are always in the index |
| Swap the VLM between something that works on a laptop and the real model on the server, without changing any other code | `vlm_caption.py` — abstract `VLMCaptioner` base + 4 backends + `make_captioner` factory |
| Avoid surprise network calls when the real VLM is running (server time is expensive) | `prepare_images.py` — pre-fetch the eval slice's images once before the run |
| Cosine similarity top-K (the only retrieval primitive we need) | `retrieve.py` — ~60 lines of NumPy. Because embeddings are L2-normalized in the encoder, cosine = matmul = `db.embeddings @ q` |
| Recall@1/5/10/50 + rank stats + per-query dump for hand-classification later | `eval.py` — `compute_metrics()` + `write_qualitative()` with a `failure_category` stub field |
| One command that runs the whole pipeline, auto-builds the DB if missing, refuses to silently reuse a stale DB | `run_baseline.py` — `ensure_caption_db()` (with two stale-DB gates) + the eval loop |

### TL;DR mental model

**Method A** = turn `(reference image + modification text)` into an
"imagined target caption" via a VLM, then retrieve via text-to-text
similarity against pre-encoded target captions. Every file in
`src/baseline/` does **one** step of that pipeline; `run_baseline.py`
ties them together. The VLM is the only piece that needs the GPU
server; everything else is plumbing that runs on the laptop.

---

## Milestone 1 — Workspace setup + FACap Dataset class

### What was done

- Sibling-cloned `speechQwen2VL` to `~/CSprojects/speechQwen2VL/` for
  read-only reference (model-loading patterns, prompt format, generation
  kwargs).
- Created a dedicated **conda** env (`fashion_retrieval`, Python 3.10.18)
  with **GPU** torch — explicitly *not* the previous
  `data_exploration/venv/`, and *not* the `speech_qwen2vl` env.
  Versions pin to `~/CSprojects/speechQwen2VL/requirements.txt` so the
  same packages are valid in either project on the eventual server.
- Implemented `src/data/facap_dataset.py` — a `torch.utils.data.Dataset`
  over FACap dress triplets that returns image *paths* (not loaded
  PIL objects), with a `load_image(item, side)` helper for spot checks.

### Files created / modified

```
src/__init__.py                        (created, empty)
src/data/__init__.py                   (created, empty)
src/baseline/__init__.py               (created, empty)
src/data/facap_dataset.py              (created)
.gitignore                             (modified: + runs/)
Documentation/Plan_2_20260427.md       (modified: conda env replaces venv)
```

### Commands run

```bash
# env
conda create -n fashion_retrieval python=3.10.18 -y
conda activate fashion_retrieval
pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 \
    torchaudio==2.4.1+cu121 \
    --extra-index-url https://download.pytorch.org/whl/cu121

# verify GPU
python -c "import torch; print(torch.cuda.is_available(), torch.version.cuda)"
# -> True 12.1   (driver = 12.7, RTX 4070 8 GB)

# sibling clone
cd ~/CSprojects && git clone https://github.com/ZhuoyuanJiang/speechQwen2VL.git

# M1 exit criterion
python -m src.data.facap_dataset --print 20
```

### Exit criterion result — PASS

- `python -m src.data.facap_dataset --print 20` printed 20 well-formed
  triplet dicts (`candidate_image_path`, `modification_text`,
  `target_image_path`, `target_caption`, `target_id`, `candidate_id`).
- `load_image()` opened `target_id=66179572_2` from
  `data_exploration/datasets/facap-images/`: size `(167, 280)`, mode
  `RGB`. Cache resolution works.
- `FacapDataset(category="dress", split="train")` reports 59,082
  triplets and 59,082 image captions (matches the FACap repo).

### Deviations from Plan_2

- Plan_2's original step 2 said "Extend the existing venv at
  `data_exploration/venv/`" with CPU-only deps. **Replaced** with a
  dedicated `fashion_retrieval` conda env + GPU torch, after a user
  course-correction. Plan_2 was patched in-place (steps 2 + 9 +
  Milestone 1 bullet, plus a new "Conda env for the baseline" subsection
  under "Critical files").
- Item dict adds a `candidate_id` field not in the original Plan_2 dict
  spec. Useful for the M3 prefetcher and qualitative dump.

---

## Milestone 2 — Text encoder + caption DB

### What was done

- Implemented `src/baseline/text_encoder.py` — a thin wrapper around
  Sentence-BERT `all-MiniLM-L6-v2`. Fixed to CPU (VRAM is reserved for
  the eventual VLM). Returns L2-normalized float32 arrays so cosine
  similarity at retrieval time is one matmul.
- Implemented `src/baseline/build_caption_db.py` — encodes a chosen
  subset of FACap target captions into a retrieval DB at
  `runs/<run_name>/caption_db/{embeddings.npy, metadata.jsonl, config.json}`.
- The smoke DB construction: **(50 eval-targets) + (950 random
  distractors)**, so every eval query's true target is guaranteed to
  be in the index. Without this, a smoke Recall@K of 0 would be
  ambiguous between "retrieval bug" and "target absent from index"
  (an issue raised in the cross-review of Plan_2).

### Files created / modified

```
src/baseline/text_encoder.py           (created)
src/baseline/build_caption_db.py       (created)
runs/smoke/caption_db/                 (generated; gitignored)
```

### Commands run

```bash
python -m src.baseline.build_caption_db \
    --run-name smoke --eval-n 50 --db-size 1000
```

### Exit criterion result — PASS

```
runs/smoke/caption_db/embeddings.npy   shape=(1000, 384) dtype=float32
runs/smoke/caption_db/metadata.jsonl   1000 rows
runs/smoke/caption_db/config.json      encoder=sentence-transformers/all-MiniLM-L6-v2
                                       subset="dress_train: last 50 triplets'
                                              targets (50 unique) +
                                              950 random distractors (seed=42)"
                                       facap_commit_sha=ac8bbc5045992a923e8933e64fd2d1487a0c4eac
                                       seed=42
```

- Row 0 norm = 1.0 (L2-normalization confirmed).
- Sources split = `{eval_target: 50, distractor: 950}`.

### Deviations from Plan_2

- None of substance. Implementation matches Plan_2 step 5.

---

## Milestone 3 — VLM captioners + retrieve + eval + orchestrator + image prefetch

### What was done

- Implemented `src/baseline/vlm_caption.py` — abstract `VLMCaptioner`
  + four concrete backends behind one interface so the orchestrator
  never branches:
  - `MockVLMCaptioner` — local templated string. Verifies plumbing.
  - `OracleCaptioner` — returns `item["target_caption"]`. Verifies
    encoder/index/retrieve/eval are bug-free.
  - `Qwen2VLCaptioner` — vanilla `Qwen/Qwen2-VL-7B-Instruct` text-only.
    Reference baseline number, server-only.
  - `SpeechQwen2VLCaptioner` — `DanJZY/Qwen2-VL-7B-Speech` base + LoRA
    adapter, in text-only mode (audio path ignored). User's chosen
    headline baseline; preserves Stage 1 → Stage 2 narrative.
  - Both real backends share `PROMPT_TEMPLATE` + `GENERATION_KWARGS`
    as class constants and gate on VRAM in `__init__` (raise clearly
    on this 8 GB laptop, would load on the server).
- Implemented `src/baseline/prepare_images.py` — pre-fetches reference
  images for the eval slice's `candidate_id`s from the
  Marqo/fashion200k HF mirror. Real VLM inference must not depend on
  surprise network calls mid-run.
- Implemented `src/baseline/retrieve.py` — `CaptionDB.load()` +
  `top_k(query_emb, db, k)` + `rank_of(true_id, query_emb, db)`.
  Uses argpartition for top-K (linear in N).
- Implemented `src/baseline/eval.py` — `compute_metrics(ranks)` →
  Recall@1/5/10/50 + median rank + mean rank, plus
  `write_qualitative()` that emits one row per query with
  `failure_category` field stub for later by-hand classification.
- Implemented `src/baseline/run_baseline.py` — top-level orchestrator
  with the CLI from Plan_2. Auto-builds the caption DB at
  `runs/<run_name>/caption_db/` if missing, so the smoke commands
  from Plan_2 step 9 are self-contained.

### Files created / modified

```
src/baseline/vlm_caption.py            (created)
src/baseline/prepare_images.py         (created)
src/baseline/retrieve.py               (created)
src/baseline/eval.py                   (created)
src/baseline/run_baseline.py           (created)
runs/smoke_oracle/                     (generated; gitignored)
runs/smoke_mock/                       (generated; gitignored)
```

### Commands run

```bash
python -m src.baseline.run_baseline --vlm oracle --n-eval 50 --run-name smoke_oracle
python -m src.baseline.run_baseline --vlm mock   --n-eval 50 --run-name smoke_mock

# server-only sanity
python -c "from src.baseline.vlm_caption import Qwen2VLCaptioner; Qwen2VLCaptioner()"
python -c "from src.baseline.vlm_caption import SpeechQwen2VLCaptioner; SpeechQwen2VLCaptioner()"
```

### Exit criterion result — PASS

**Oracle smoke (50 queries).** Identity-path sanity check passed.

```
n queries:    50
unranked:     0
median rank:  1.0
mean rank:    1.00
R@1           1.0000
R@5           1.0000
R@10          1.0000
R@50          1.0000
```

Plan_2's bar was `R@1 ≥ 0.95`. We hit perfect 1.0. **What this proves
and doesn't:** the oracle backend returns `item["target_caption"]`
verbatim, and the smoke DB construction includes that exact caption as
an `eval_target` row. So a perfect score confirms only that the
exact-text encode → retrieve → rank path works on this smoke setup —
it does not exercise non-identical query captions and would not catch
issues like stale-DB reuse under the same `run_name` (codex review
round 1 raised both points; the latter is now caught at runtime in
`run_baseline.ensure_caption_db`). Treat oracle as a *floor* for
plumbing correctness, not a ceiling.

**Mock smoke (50 queries).** Numbers don't matter; what matters is
that the pipeline runs to completion and writes the artifacts.

```
n queries:    50
unranked:     0
median rank:  7.0
mean rank:    44.54
R@1           0.2000
R@5           0.4200
R@10          0.6000
R@50          0.7800
```

Mock numbers are higher than expected (R@1 = 0.20 instead of near
random ~0.001 for a 1000-row DB). The reason is mechanical: the mock
template is `"A fashion item that is {modification_text}"` and the
modification text in FACap describes the *target* garment in detail,
which SBERT picks up against the target's caption. Not a bug — just
a fact about the mock template being a soft floor, not a true
random-baseline floor. We didn't tighten this because the milestone
exit criterion is "exits clean", not "mock numbers are random". The
oracle's perfect score is the one that actually proves correctness.

**Real-backend server-only gating.**

```
Qwen2VLCaptioner: server-only: Qwen2-VL-7B-Instruct (vanilla) needs
>= 14.0 GB VRAM at bf16; this machine has 8.0 GB. Run on the GPU server.

SpeechQwen2VLCaptioner: server-only: speechQwen2VL (Stage-1 base +
Stage-2 LoRA) needs >= 14.0 GB VRAM at bf16; this machine has 8.0 GB.
Run on the GPU server.
```

Both classes import without error; instantiation raises clearly. The
threshold (14 GB) is hard-coded as
`MIN_VRAM_GB_FOR_QWEN2VL_7B = 14.0` in `vlm_caption.py` — if a future
quantization changes the footprint, drop it there.

### Deviations from Plan_2

- `run_baseline.py` **auto-builds the caption DB** at
  `runs/<run_name>/caption_db/` if it doesn't exist, instead of
  requiring a separate `build_caption_db` step before each run. This
  makes Plan_2 step 9's smoke commands self-contained as written.
  `build_caption_db.py` is still independently runnable for the
  full-DB server use case.
- `MockVLMCaptioner` template is `"A fashion item that is {mod}"` —
  shorter than the Plan_2 sketch (`"... derived from {brief
  description of reference}"`). The longer template would have needed
  a vision pass to describe the reference, which defeats the point of
  a CPU-only mock. Result: mock numbers are slightly higher than a
  true random baseline (see above) but this didn't affect the
  exit-criterion check.
- `qualitative/results.jsonl` rows include extra fields beyond
  Plan_2's spec: `query_idx`, `modification_text`, `top10_scores`,
  `rank`. They cost nothing to write and make hand-classification of
  failures easier later.
- **`max_new_tokens` bumped 128 → 256** in `_Qwen2VLLikeCaptioner.GENERATION_KWARGS`
  (2026-04-30, post-M3, after the prompt-design analysis below).
  Rationale: FACap's longest dress caption is ~170 tokens
  (130 words × ~1.3 tokens/word); a 128-token cap would silently
  truncate any prompt that successfully elicits a FACap-length
  caption. With greedy decoding (`num_beams=1`, `do_sample=False`),
  raising the cap is free on outputs that finish early under
  "concise" — it only matters when the model wants to keep going.
  Pure safety margin; doesn't change behavior on the current
  zero-shot prompt.

### Prompt design considerations for Plan_3

The current prompt is zero-shot — class constants at
`vlm_caption.py:78–88`:

```python
PROMPT_TEMPLATE = (
    "Given the reference fashion image and the modification instruction, "
    "write a concise caption describing the target fashion item after "
    "applying the modification."
)
GENERATION_KWARGS = {"max_new_tokens": 128, "num_beams": 1, "do_sample": False}
```

> **Update (2026-04-30, post-analysis).** `max_new_tokens` was
> subsequently bumped to **256** — see the corresponding deviations
> bullet above. The analysis below references the original `128`
> because the gap argument concerns the binding *prompt* constraint
> (`"concise"`), not the generation cap. The bump is a pure safety
> margin against silent truncation if a future prompt elicits longer
> outputs; under the current zero-shot prompt + greedy decoding it
> changes nothing.

A real query is `[image] + PROMPT_TEMPLATE + "\n\nModification: {mod}"`.
Detailed treatment lives in `Documentation/notes_baseline.md` Q9; the
parts that bear on Plan_3 specifically:

**FACap caption-length distribution (empirical, n=1000 dress
targets, measured from `runs/smoke_oracle/caption_db/metadata.jsonl`).**

| Stat | chars | ~words |
|---|---|---|
| min | 201 | 40 |
| p10 | 427 | 85 |
| median | 566 | 113 |
| mean | 538 | 108 |
| p90 | 615 | 123 |
| max | 651 | 130 |

FACap dress captions are **~110 words median**, range mostly 85–125
words — longer than a "80-word" rule-of-thumb estimate.

**Why length matters for SBERT retrieval.** `all-MiniLM-L6-v2` is a
**symmetric** encoder using mean-pooling — the 384-d vector reflects
all topics in the input, weighted by frequency. The right framing is
**topical-scope mismatch**, not length per se. A long caption with
extra topics not in the target *dilutes* the relevant topics; a short
caption misses topics the DB caption has. The current `"concise"`
instruction + `max_new_tokens=128` (~95-word ceiling) will likely
yield 30–60 word outputs against the ~110-word-median DB above — a
real gap.

**Should we match DB style?** Mostly yes — both sides are
fashion-item captions (symmetric retrieval). Match length/scope,
fashion-specific vocabulary (silhouette, neckline, hemline), and
attribute order. Don't match generic boilerplate (*"This is a..."* /
*"The image shows..."*) which cancels out, and don't hallucinate
content to fill space (lands embeddings randomly).

**Standard moves in IR / RAG.**

| Move | Idea | When to use |
|---|---|---|
| **Symmetric encoder + style-matched prompt** | Few-shot or explicit style instructions to nudge VLM output toward DB style; encode both with the same SBERT-style model | Our case: DB and queries are same content type |
| **Asymmetric encoder** | Use a model trained with separate "query:" / "passage:" prefixes (BGE, E5, msmarco-MiniLM) that learns to bridge query↔doc style differences | When queries are short / questions but docs are long — not our case |
| **Two-stage rerank** | Dense first-stage retrieves top-50; a cross-encoder reranks them | When first-stage Recall@50 is good but Recall@1 is poor |
| **Hybrid (BM25 + dense)** | Sparse (BM25) + dense (SBERT) scores combined | When key attributes are rare/specific words (brand names, exact colors) |
| **Few-shot prompting** | Show 2–3 (image, modification, ideal_caption) triplets in the prompt | Standard cheap fix to nudge output style |

The professional default for composed image retrieval via captions
is roughly: (1) symmetric SBERT-family encoder both sides ✅,
(2) style-matched prompt, (3) few-shot if needed,
(4) cross-encoder reranker, (5) swap encoder only if 1–4 exhausted.

**Decision (2026-04-30): zero-shot first, evidence-driven
iteration.** Hardcoding a style instruction now (e.g. *"~80 words,
mention color, silhouette, fabric, length, neckline"*) feels too
prescriptive *before we have a number*. For Plan_3:

1. Run zero-shot (current `PROMPT_TEMPLATE`) → first metrics +
   `qualitative/results.jsonl`.
2. Read ~5 generated captions. If they're 30-word generic vs.
   FACap's 110-word fashion-rich, the style mismatch is real and
   visible in the data.
3. Iterate the prompt with style guidance only if the numbers
   warrant it.

Principle: don't optimize a prompt against an *imagined* problem.
Run the experiment, look at the failures, then prescribe.

---

## Test suite — `tests/`

Added 2026-04-30, post-M3 cross-review. Formalizes the M1–M3 smoke
commands as a persistent, runnable record so a future contributor (or
six-months-from-now me) can answer *"is this still working?"* without
re-deriving the exit-criterion checks from scratch.

### Layout

```
tests/
├── _runner.py                  # shared script-mode runner
├── test_m1_facap_dataset.py    # 5 cases
├── test_m2_caption_db.py       # 5 cases
└── test_m3_pipeline.py         # 3 cases
```

Each file is dual-mode — pure Python with `def test_*()` + plain
`assert`s, plus a script-runner block at the bottom:

```bash
python -m tests.test_m2_caption_db   # script mode, prints ✓/✗ per case
pytest tests/                         # pytest also works (optional dep)
```

### What each test verifies

- **M1** — `FacapDataset` constructs against the on-disk FACap repo;
  items are 6-key dicts with the documented schema; field types are
  strings; `target_id` is correctly derived from the path; `load_image()`
  opens at least one cached image.
- **M2** — `embeddings.npy` is `(1000, 384)` float32 with L2-normalized
  rows; `metadata.jsonl` has 1000 rows with the documented fields;
  source split is `eval_target=50, distractor=950`; `config.json`
  records encoder, `build_args`, and a 40-char `facap_commit_sha`
  (the inputs the stale-DB gate reads).
- **M3** — oracle hits perfect Recall@1 (proves the
  encoder/index/retrieve/rank chain is bug-free); mock runs to
  completion and writes `metrics.json` (with the nested `recall` dict)
  + `qualitative/results.jsonl` carrying every documented field
  including the blank `failure_category` stub; both real backends
  (`Qwen2VL`, `SpeechQwen2VL`) raise a clear server-only RuntimeError
  on this 8 GB laptop, confirming the VRAM gate is healthy.

The M3 tests caught one bug during scaffolding — I'd guessed at
`metrics.json` field names (`recall_at_1`, `n_queries`) without
checking, and the real schema is nested (`recall["R@1"]`, `n`). Tests
caught the assumption immediately, which is exactly their point.

### Status (2026-04-30)

13/13 passing. Tests build into fresh `runs/_test_*/` directories so
they don't collide with the user's smoke runs (all under the same
gitignored `runs/`).

### Note on pytest

`pytest 9.0.3` is installed locally but **not in `environment.yml` /
`requirements.txt`** — it's a personal dev tool, and the test files
themselves don't depend on it (they use plain `assert` plus the
`_runner.py` script harness). If a future contributor wants pytest,
`pip install pytest` is enough.

---

## What's deferred (M4 and beyond)

- **M4** — README narrative for Stage 1 → Stage 2, real-VLM interface
  polish, `docs/server_handoff.md`, commit/push. Held back so M4 stays
  one clean milestone separate from the M1–M3 commits.
- **Plan_3** — Server-side run with the real `speechQwen2VL` backend
  on the FACap dress evaluation slice. Includes
  `Documentation/Baseline_1_Report_<YYYYMMDD>.md` (metrics +
  qualitative pairs + interpretation).

## Verification snapshot

For anyone reproducing M1–M3 from a fresh clone of the env:

```bash
conda activate fashion_retrieval
python -m src.data.facap_dataset --print 20
python -m src.baseline.run_baseline --vlm oracle --n-eval 50 --run-name smoke_oracle
python -m src.baseline.run_baseline --vlm mock   --n-eval 50 --run-name smoke_mock
```

Expected: oracle R@1 = 1.0 on the identity-path smoke setup, mock
exits with metrics printed, both write
`runs/smoke_*/{caption_db/, metrics.json, qualitative/results.jsonl}`
(all gitignored).

`run_baseline.py` gates against stale-DB reuse on two axes:
1. **CLI args.** Rerunning with the same `--run-name` but a different
   `--encoder`, `--n-eval`, `--db-size`, `--seed`, `--category`, or
   `--split` raises `RuntimeError` with a diff of the offending args.
2. **FACap data revision.** If the local `data_exploration/datasets/facap-repo/`
   HEAD changes between when the DB was built and the current run, the
   gate also raises (the recorded `facap_commit_sha` no longer matches
   the current checkout).

In either case, delete the run dir or pick a fresh `--run-name` to
proceed.

## Notes for the next round

- The mock template is a **soft floor**, not a random floor. If we
  ever want a true random baseline for comparison, add a
  `RandomCaptioner` that emits an unrelated string (or a random row
  from the captions) — useful as a second sanity number alongside
  oracle.
- `sentence-transformers` pulled upstream `transformers==5.6.2` as
  a transitive dep. On the server (Plan_3), the `speechQwen2VL`
  forked `transformers` must be installed **last** if we share one
  env, OR the real VLM runs in `speech_qwen2vl` and only the
  caption-DB / retrieval / eval pieces run in `fashion_retrieval`.
- `runs/` is gitignored as of this round. Numbers go in this
  Progress doc + future Baseline reports; the artifacts stay local.

## Milestone 4 — Server handoff package

(Appended 2026-04-30. The "What's deferred" section above tagged M4 as
not-yet-done; this section is what actually shipped. The original
section is preserved as-written for traceability.)

### What was done

- **environment.yml rewrite.** Now mirrors `speechQwen2VL/environment.yml`
  exactly at the conda layer (`python=3.10.18`, `cuda-toolkit=12.1.1`,
  `ffmpeg=8.0.1`, `libsndfile=1.2.2`). The pip layer is delegated to
  `requirements.txt`. The conda layer is the place where mismatches are
  most painful (CUDA / Python ABI / system libs), so we make the two
  envs structurally identical there and let any divergence happen only
  in the pip layer where it's cheap to debug.
- **requirements.txt rewrite.** Hand-curated as a strict superset of
  speechQwen2VL's pip pins. All upstream pins mirrored (`torch
  2.4.1+cu121`, `peft 0.17.1`, `datasets 4.0.0`, `accelerate 1.10.0`,
  `huggingface-hub 0.34.4`, `tokenizers 0.21.4`, `safetensors 0.6.2`,
  `bitsandbytes 0.47.0`, `flash-attn 2.6.3`, `librosa`, `soundfile`,
  `jiwer`, `wandb`, `tensorboard`, etc.) plus Stage-2 additions
  (`sentence-transformers==3.4.1` and `requests==2.32.3`).
  `sentence-transformers` is pinned at 3.x because 5.x requires
  `huggingface-hub 1.x`, which would conflict with speechQwen2VL's
  `0.34.4` pin. The forked `transformers` + `qwen-vl-utils` are
  intentionally **NOT** in this file — they get installed last by
  `setup_forks.sh` so they override the upstream `transformers` that
  `sentence-transformers` pulled in.
- **scripts/setup_server.sh.** One-shot env setup. Auto-clones
  `speechQwen2VL` alongside if missing (configurable via
  `SPEECHQWEN2VL_REPO` env var, default = the public repo URL),
  creates the conda env from `environment.yml`, runs `pip install
  -r requirements.txt`, then **shells out to**
  `../speechQwen2VL/scripts/setup_forks.sh`. Single source of truth
  for the fork SHAs — if speechQwen2VL bumps its forked transformers,
  we get the bump for free with no drift.
- **scripts/run_baseline_v1.sh.** One command for the headline run.
  Defaults: `n_eval=1000`, `db_size=59082`, `vlm=speechqwen2vl`,
  `run_name=baseline_v1_speechqwen2vl`. Three steps: prefetch images
  → run baseline → pretty-print `metrics.json`. All knobs overridable
  via env vars (`N_EVAL`, `DB_SIZE`, `VLM`, `RUN_NAME`, `HF_HOME`).
- **README.md update.** Added a "Real VLM baseline on a server"
  section between the existing "Running the baseline" and "Repo
  structure" sections. Covers server prerequisites (>= 14 GB VRAM,
  HF_TOKEN, ~20 GB disk), one-shot setup (sibling clone +
  `bash scripts/setup_server.sh`), `HF_HOME` override for scratch
  disks, one-shot run (`bash scripts/run_baseline_v1.sh`), output
  structure, and a troubleshooting subsection. Existing README
  sections left untouched.
- **Handoff prompt (not committed).** The two bits that are
  task-specific to *this* server-handoff rather than project-doc
  material live in a separate paste-ready prompt: (1) the "should I
  delete the existing `speech_qwen2vl` env?" trade-off (keep if
  revisiting Stage-1; delete to save ~13 GB if Stage-1 is shipped —
  safe because the trained weights live on HuggingFace as
  `DanJZY/Qwen2-VL-7B-Speech` + `-LoRA`), and (2) the spec for the
  deliverable (`Documentation/Baseline_1_Report_<YYYYMMDD>.md`). An
  earlier draft `docs/server_handoff.md` consolidated both into a
  committed file, but ~70% of it duplicated the README's general
  setup info; the remaining 30% reads better as an agent task brief
  than as a permanent doc, so the prompt is what ships.
- **notebooks/baseline_demo.ipynb.** Portable end-to-end demo.
  Auto-detects Colab vs server in cell 0; on Colab it clones both
  repos and runs `setup_forks.sh`, on server it just verifies imports
  and uses the active env. Calls `run_baseline()` Python API
  directly (not shell-out) so the cells read top-to-bottom. Cells:
  framing → setup → run config → sanity dataset → sanity oracle (n=50)
  → prefetch images → real baseline run → metrics table → qualitative
  grid (3 success + 3 failure with PIL+matplotlib) → run-notes
  template. Outputs left as placeholders in the committed version;
  running it fills them in.

### Files created / modified

- `environment.yml` — modified (shipped in earlier commit `Update env`)
- `requirements.txt` — modified (shipped in earlier commit `Update env`)
- `README.md` — modified (new "Real VLM baseline on a server" section)
- `scripts/setup_server.sh` — created (executable, +x)
- `scripts/run_baseline_v1.sh` — created (executable, +x)
- `notebooks/baseline_demo.ipynb` — created

### Why "mirror the speechQwen2VL conda layer"

speechQwen2VL pins its conda layer at `python=3.10.18,
cuda-toolkit=12.1.1, ffmpeg=8.0.1, libsndfile=1.2.2`. By using the
exact same pins in fashion-retrieval-agent's `environment.yml`, the
two envs are structurally identical at the conda layer — same Python
ABI, same CUDA ABI, same audio system libs. The only place they
diverge is the pip layer, which is explicitly designed as a superset
(speechQwen2VL's pins + `sentence-transformers` + `requests`).

Practical consequence: someone who deletes `speech_qwen2vl` to save
~13 GB can fall back to running Stage-1 audio code in
`fashion_retrieval` if they ever need to — the audio system libs are
present at the conda layer, the audio pip deps (`librosa`,
`soundfile`, `jiwer`) are present at the pip layer, and Stage-1
training-only deps (`bitsandbytes`, `flash-attn`, `accelerate`,
`trl`) are too. This is what makes the handoff prompt's "delete
`speech_qwen2vl` to save ~13 GB" recommendation safe.

### Exit criterion result — DEFERRED to Plan_3

M4's exit criterion is "scripts run cleanly on the server, baseline
produces a `metrics.json`". This **cannot** be verified locally (no
GPU >= 14 GB VRAM here). It will be verified by Plan_3 when the
server agent runs `bash scripts/run_baseline_v1.sh`. If anything in
the M4 scaffolding turns out to be broken on the server, fix-ups
land in a follow-up commit before the Baseline report is written.

### Deviations from Plan_2

- Plan_2 said `README.md` skeleton with Stage 1 → Stage 2 narrative
  was an M4 deliverable. **Done as an additive update to the existing
  README** (the repo already had a substantive README from earlier
  rounds). The new section covers server prerequisites + one-shot
  setup + one-shot run + troubleshooting. The Stage 1 → Stage 2
  narrative was already in the existing README intro, so no rewrite
  needed there.
- Plan_2 said "fully wire `Qwen2VLCaptioner` and
  `SpeechQwen2VLCaptioner` interfaces, prompt template hard-coded as
  a class constant" was an M4 task. **Done in M3** (one milestone
  earlier than planned), so M4 inherited working captioner code with
  no further changes there.
- Added two scripts (`setup_server.sh` + `run_baseline_v1.sh`) that
  Plan_2 didn't explicitly call for. Reason: a paste-ready bash flow
  makes the server agent's job mechanical instead of cognitive. The
  trade-off is two more files to maintain, but they're thin wrappers
  over the underlying Python entry points.
- Notebook scope grew beyond Plan_2's "optional presentation
  notebook" mention — it now auto-detects Colab vs server so the same
  artifact works in both environments. Cost: ~30 lines of
  environment-detection boilerplate in cell 0. Benefit: one notebook
  to maintain instead of two.
